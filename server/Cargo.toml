[package]
name = "web-socket-server"
version = "0.1.0"
edition = "2021"

[dependencies]
bin-layout = "7"
tokio = { version = "1.20.0", features = ["full"] }
ws-proto = { version = "0.1.0", path = "../protocol" }
